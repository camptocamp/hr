<?xml version="1.0" encoding="UTF-8"?>
<odoo>

  <record id="rule_crm_lead_program_manager" model="ir.rule">
    <field name="name">Program Manager Leads</field>
    <field ref="model_crm_lead" name="model_id"/>
    <field name="domain_force">[('program_manager_id','=',user.id)]</field>
    <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
  </record>

</odoo>
