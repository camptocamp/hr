<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="constraints_hr_holidays_form" model="ir.ui.view">
        <field name="name">hr.holidays.constraints</field>
        <field name="model">hr.holidays</field>
        <field name="inherit_id"
               ref="hr_holidays.edit_holiday_new"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='employee_id']" position="before">
                <field name="employee_is_user" invisible="1"/>
            </xpath>

            <xpath expr="//button[@name='action_approve']"
                   position="attributes">
                <attribute name="states"/>
                <attribute name="attrs">
                    {'invisible': [
                    '|',
                    ('state', 'not in', ('confirm')),
                    ('employee_is_user', '=', True)
                    ]}
                </attribute>
            </xpath>

            <xpath expr="//button[@name='action_refuse']"
                   position="attributes">
                <attribute name="states"/>
                <attribute name="attrs">
                    {'invisible': [
                    '|',
                    ('state', 'not in', ('confirm', 'validate', 'validate1')),
                    ('employee_is_user', '=', True)
                    ]}
                </attribute>
            </xpath>
        </field>
    </record>

</odoo>
