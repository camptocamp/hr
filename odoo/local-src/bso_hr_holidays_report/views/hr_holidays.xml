<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="monthly_report_hr_holidays_form" model="ir.ui.view">
        <field name="name">hr.holidays</field>
        <field name="model">hr.holidays</field>
        <field name="inherit_id" ref="hr_holidays.edit_holiday_new"/>
        <field name="arch" type="xml">
            <field name="number_of_days_temp" position="attributes">
                <attribute name="attrs">{'invisible':[('type', '=', 'remove')]}
                </attribute>
            </field>
            <field name="number_of_days_temp" position="after">
                <field name="number_of_days"
                       attrs="{'invisible':[('type', '!=', 'remove')]}"/>
            </field>
        </field>
    </record>

</odoo>
